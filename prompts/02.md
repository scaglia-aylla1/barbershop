## Tarefa

Criar tela que está em (url da interface no figma) usando Figma MCP.

Caso o usuário esteja deslogado, exiba a interface (url da interface no figma)
Tudo deve ser feito em apenas um componente.

## Requisitos e Detalhes Técnicos

- O avatar, nome e e-mail devem ser do usuário logado.
- Botão de início deve levar para tela inicial.
- Botão de agendamentos deve levar para página "/bookings" (NÃO CRIE ESSA PÁGINA AINDA)
- Os botões de categoria (Cabelo, barba etc.) NÃO DEVEM FAZER NADA, POR ENQUANTO.
- Botão de "Sair" deve fazer logout.
- Use o componente Sheet do shadcn para exibir o componente.
- Esse componente deve ser aberto ao clicar no botão de Menu que está no @app/\_components/header.tsx na linha 19.